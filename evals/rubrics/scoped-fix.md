# Scoped Fix Rubric

- pass only when bounded implementation work routes to Ralph
- fail when required task specs or checkpoint artifacts are missing
- fail when the deterministic loop surface does not bootstrap cleanly
