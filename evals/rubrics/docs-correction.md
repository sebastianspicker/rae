# Docs Correction Rubric

- pass only when bounded documentation correction routes to Ralph
- fail when the deterministic loop does not emit the expected embedded runtime artifacts
- fail when required checkpoints are not approved
