# Observability Scenarios

Use this family for tasks where the system must emit operator-facing status
artifacts instead of requiring raw trace inspection.

Typical metrics:

- summary artifact completeness
- blocker/next-action visibility
- activity/cost rollup availability
