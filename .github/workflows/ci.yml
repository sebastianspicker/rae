name: verify
on:
  push:
    branches: [dev]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
mkdocs: bootstrap
docker: bootstrap
github-actions: core-build-out
align-local-setup-comman: expansion
