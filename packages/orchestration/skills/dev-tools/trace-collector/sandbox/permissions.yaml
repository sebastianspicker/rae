# Keep trace parsing offline and prevent mutation of the mounted workspace.
network:
  allow: []
filesystem:
  read:
    - /workspace
  write:
    - /tmp
process:
  allow_subprocess: false
limits:
  max_files: 5000
  max_file_mb: 5
